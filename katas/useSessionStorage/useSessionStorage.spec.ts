import { describe, it, expect } from "vitest";

describe("useSessionStorage", () => {
	it("should...", () => {
		//TODO: add tests case by case useSessionStorage hook
		expect(true).containSubset(true);
	});
});
